name: memos-toolset
version: "1.0.3"
description: MemOS memory store, search, and auto-capture — persistent vector memory for agents
author: openclaw
requires_env:
  - MEMOS_API_KEY
  - MEMOS_USER_ID
  - MEMOS_CUBE_ID
provides_tools:
  - memos_store
  - memos_search
provides_hooks:
  - post_llm_call
